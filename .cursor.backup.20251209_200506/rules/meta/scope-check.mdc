---
description: Validates that rule files are placed in the correct category folders (general, frontend, backend, workflow, meta) to maintain proper organization and scope.
globs: 
alwaysApply: false
---
rule "Rule File Scope" {
  type: "manual"
  when: file.path matches ".cursor/rules/(general|frontend|backend|workflow|meta).*"
  valid: "✅ Scoped rule file."
}
